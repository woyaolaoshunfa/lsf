``` 
echo "# lsf" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/woyaolaoshunfa/lsf.git
git push -u origin master
```

```
#添加
git add . 
#提交
git commit -m "备注信息" 
#发布到master
git push -u origin master
```

